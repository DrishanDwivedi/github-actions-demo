name: ci workflow
on:
    push:
jobs:
    build: #Job ka naam
        runs-on: ubuntu-latest #Github Runner OS (Free)
        steps:
            - name: Checkout code #Step ka naam 
              uses: actions/checkout@v4  #Clone the code into Ubuntu runner
            - name: Set up Docker
              uses: docker/setup-buildx-action@v4 #Docker setup action
 